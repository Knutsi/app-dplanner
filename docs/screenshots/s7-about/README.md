# About DPlanner (S7)

Rendered by `uv run python scripts/render_about.py --out docs/screenshots/s7-about`.

| Image | What it shows |
|---|---|
| `about-*` | Help ▸ About DPlanner: the name and version, then what it is built on — a row per component with the version and the licence, both read from the installed distribution's own metadata rather than written down. |

Dark and light (`-dark`, `-light`).
