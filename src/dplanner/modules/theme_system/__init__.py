"""The desktop's own dark or light as a theme provider — macOS, Windows, GNOME and KDE —
in ``themes.py``.

Qt-free on purpose: the composition root hands it the readings Qt has, and lists it in
``theme_providers()``.
"""
