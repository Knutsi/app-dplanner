"""Coverage: the spec, and what became of it — passages → features → milestones → tests
and docs, drawn as four columns joined by lines, and printed by ``dplanner coverage``.

``trace.py`` is the Qt-free picture both surfaces read; ``cli.py`` the verbs; ``module.py``
and ``scene.py`` the tab. The composition root imports the Qt half from ``module``.
"""
