"""The window's half of reporting: File ▸ Export's report entries, Preview Report, the
publisher that writes the plan repository's site on every Save, the PDF (``paper.py``) and
Settings ▸ Reports. The report itself — what modules say, the page, the site, the verbs —
is ``dplanner.cli.report``, Qt-free, so the terminal writes the same file this window does.
"""
